const config = {
    SERVER_PORT: 8000,
    DB_NAME: "test",
    DB_URL: "localhost",
    DB_PORT: 27017,

}
export enum COLLECTTIONS {
    users = 'users',
    tokens = 'tokens'

}
export default config;